# Marketing site (moved to repo root)

This README contains the marketing site scaffold that was moved into the repository root from `website-marketing/`.

Quick start

```pwsh
cd c:\Users\asplo\Documents\GitHub\pinglo-website
npm install
Copy-Item .env.local.example .env.local
# edit .env.local with your Firebase credentials
npm run dev
```

Files added to root: package.json, .env.local.example, and `src/` containing the marketing site app.
